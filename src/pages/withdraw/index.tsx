/* eslint-disable complexity */
import React from 'react';
import Wrapper from '../../components/withdrow/Wrapper'

import { useRouter } from 'next/router';

function Withdraw() {
  const router = useRouter();
  return (
    <Wrapper />
  );
}

export default Withdraw;
